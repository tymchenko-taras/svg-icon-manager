import { SvgParser } from '../../src';
import SimplePreviwer from '../../src/previewer/SimplePreviwer';

describe('index', () => {
  describe('myPackage', () => {
    it('test', () => {
      const parser = new SimplePreviwer('E:/www/react-svg-parser/src/previewer/SimpleTemplate.html');

      // expect(result).toEqual('');
    });
  });
});
